"""No request body -- this is a POST (no body, path+query only). Path/query parameters stay declared inline on the router function signature so OpenAPI `parameters` stay unchanged."""

DEFAULT_DEAL_TYPE = "BRRRR"
