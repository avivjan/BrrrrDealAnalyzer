[pytest]
testpaths = tests
# `-p no:cacheprovider` keeps a read-only deploy filesystem from failing on
# .pytest_cache; `-q` keeps the Render build log readable.
addopts = -q -p no:cacheprovider
filterwarnings =
    ignore::DeprecationWarning
