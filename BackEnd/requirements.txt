# Exact pins (SECURITY_PLAN.md F-16): a deploy installs what CI tested.
# Bump deliberately; Dependabot (.github/dependabot.yml) proposes updates and
# CI's pip-audit fails the build on a known advisory.
fastapi==0.141.1
uvicorn[standard]==0.32.1
SQLAlchemy==2.0.52
psycopg2-binary==2.9.12
reportlab==5.0.1
python-multipart==0.0.32
requests==2.34.2
email-validator==2.3.0
# Passkeys (SECURITY_PLAN.md §3.2): py_webauthn and its CBOR/TLS helpers.
webauthn==3.0.0
cbor2==6.1.4
pyOpenSSL==26.4.0
python-dotenv==1.2.3

google-api-python-client==2.200.0
google-auth==2.57.1
google-auth-httplib2==0.4.2
google-cloud-storage==3.13.1

# MCP server (mcp_server.py): exposes every endpoint as a tool for Claude connectors.
mcp==1.29.1

# Test suite (BackEnd/tests). Kept in the main requirements file because the
# Render build installs from it before running pytest in the pre-deploy step.
# httpx is what fastapi.testclient.TestClient is built on.
pytest==9.1.1
pytest-cov==7.1.0
httpx==0.28.1
