{
  "scripts": [
    {
      "file": "src/components/DealInputsForm.vue",
      "reason": "Task 3.4: `import { useId } from \"vue\";` plus `const hmToggleId = useId();` — the id the \"Use HM for Rehab\" <label for> and the ToggleSwitch's inputId share. Template-only otherwise."
    },
    {
      "file": "src/components/liquidity/TimelineChart.vue",
      "reason": "Task 3.9 (E1): `import { chartToken } from \"../../design/chartTokens\";` plus, inside draw(), each colour literal replaced in place by `chartToken('<name>')`. Nothing else on those lines moves -- G3's E1 comparator pairs every removed line with its added line and accepts only that substitution. The chart is a <canvas>, so it cannot read a Tailwind class; this is the only way its palette follows the theme."
    },
    {
      "file": "src/components/ui/DaysUntilRefiField.vue",
      "reason": "Task 3.4: `import { useId } from \"vue\";` plus `daysInputId`, `purchaseDateId` and `refiDateId` — one per control, so each of the field's three labels can point at the input it names."
    },
    {
      "file": "src/components/ui/MoneyInput.vue",
      "reason": "useId for label association (Task 3.4 round 1)"
    },
    {
      "file": "src/components/ui/NumberInput.vue",
      "reason": "Task 3.4: `import { useId } from \"vue\";` plus `const inputId = useId();` — the id shared by the <label for> and InputNumber's inputId, which is the only way to reach the <input> PrimeVue renders."
    },
    {
      "file": "src/components/ui/SliderField.vue",
      "reason": "Task 3.4: `import { useId } from \"vue\";` plus `const inputId = useId();` — same label-to-InputNumber association as NumberInput."
    }
  ],
  "bindings": [
    {
      "file": "src/App.vue",
      "reason": "routerview-transition-slot"
    },
    {
      "file": "src/views/MyDeals.vue",
      "expression": "!columns[stage.id]?.length",
      "reason": "Task 3.6: a new, purely presentational `<UiEmptyState v-if>` beside (never inside) each stage's draggable list, so an empty column says so instead of showing a blank 100px well. Reads existing state only — the same `columns[stage.id]` the row's counter already reads — and adds no handler."
    },
    {
      "file": "src/views/BoughtDeals.vue",
      "expression": "!columns[stage.id]?.length",
      "reason": "Task 3.7: the same new, purely presentational `<UiEmptyState v-if>` MyDeals grew in Task 3.6, beside (never inside) each stage's draggable list, so an empty pipeline stage says so instead of showing a blank 100px well. Reads existing state only — the same `columns[stage.id]` the row's counter already reads — and adds no handler."
    }
  ],
  "text": [
    {
      "file": "src/views/MyDeals.vue",
      "from": "",
      "to": "No deals in this stage",
      "reason": "Task 3.6: the empty-column state's only line. New copy for a new element (see the matching `bindings` row); nothing existing was reworded."
    },
    {
      "file": "src/views/BoughtDeals.vue",
      "from": "",
      "to": "No deals in this stage",
      "reason": "Task 3.7: the empty-column state's only line, word for word the one MyDeals uses (see the matching `bindings` row). `from: \"\"` because `text.mjs` reports an addition with no adjacent removal as `\"\" => \"…\"`; nothing existing was reworded."
    }
  ]
}
